# 2026-08-18 实施记录

## 架构入口校正：Pi 主链路与本地 App/云端 Web 分离

- 用户确认原始目标：Pi 是管理对话、Skill 使用与 Tool 编排的主架构入口；LLM Wiki 是外部领域组件。服务器 Agent Session 路由现以 `resolveServerAgentLoop` 默认选择 `pi`，并在 `agent_loop.routing` 审计中记录该选择。
- `WKNOWLEDGE_AGENT_LOOP=internal` 仅保留为明确设置的、短期应急回退；未设置、设置为 `pi` 或任何未知值均进入 Pi，避免把 Pi 再降级为旁路。回退不是 SQLite/local App 的运行开关。
- SQLite 的边界已重新冻结：`apps/web`/`apps/worker` 只组成 PostgreSQL + pg-boss 的云端 Web 运行档，且回归测试拒绝其 Agent 路由导入 `node:sqlite`、`createSqlite*` 或数据库 Profile 切换环境变量；SQLite Session/Queue Adapter 仅服务 M7-14 独立本地 App 组合根，不能复用 Docker Compose 或云端数据目录。
- 验证：Pi 路由与运行档边界单测 9/9；隔离 PostgreSQL + SQLite 分支根测试 104 files / 595 tests；默认 Pi 环境的 Playwright E2E 23/23；typecheck、lint、format、production build 全绿。
- 最终发布复核：包含默认 Pi 入口的 `wknowledge-app:release-check-pi`（`sha256:7f0571…`）真实 Buildx 构建成功；隔离 Compose `wknowledge-release-check-pi` 的 preflight、迁移 exited 0、PostgreSQL/Web/Worker/Gateway healthy，以及 `http://127.0.0.1:13002/api/health/ready` 均通过。临时容器、网络和卷已清理。

## M7-10-M4/M5-13：Pi 升级基线的可部署性收口

- **E2E 运行档修正**：邀请接受路由在无数据库时先经过 fail-closed 的公开写入防护，正确响应 `503 REQUEST_GUARD_UNAVAILABLE`；`apps/web/tests/home.spec.ts` 现按是否设置 `DATABASE_URL` 断言无 DB 的 503 与有 DB 的无效邀请 409，避免把基础设施缺失误写成业务回归。
- **真实数据库门禁**：隔离 PostgreSQL `wknowledge_test` 已完成 48 个迁移；`pnpm db:upgrade:check` 返回 `status=current`、末迁移 `0047_agent_skill_installations`。`DATABASE_URL + NODE_OPTIONS=--experimental-sqlite pnpm test` 通过 **104 files / 593 tests**；全量 Playwright E2E 通过 **23/23**；typecheck、lint、format 与生产 build 均通过。
- **镜像可靠性修复**：首次 Buildx 因阿里云 HTTP 包下载连接失败（`Unable to connect to mirrors.aliyun.com:80`）无法产出镜像；HTTPS 直连虽可用，但最小 `node:22.23.2-bookworm-slim` 尚无系统 CA，APT 明确报 `No system certificates available`。Dockerfile 现以已签名的 HTTP 仓库索引仅引导安装 `ca-certificates`，随后完整运行时依赖使用 **阿里云 HTTPS → Debian 官方 HTTPS** 的单次回退，全部 update/install 保持 `Acquire::Retries=3`，未关闭 TLS 或 APT 认证、未加入新镜像源、未改变运行时包集合。静态回归 5/5 通过。
- **真实发布演练**：`docker buildx build --allow network.host --load --tag wknowledge-app:release-check` 成功产出本地镜像 `sha256:c8ab…`（774,948,040 bytes）。隔离 Compose 项目 `wknowledge-release-check` 中，部署 preflight 返回 `status=ok`；PostgreSQL healthy、迁移 exited 0、Web/Worker/Gateway healthy，网关 `http://127.0.0.1:13001/api/health/ready` 返回 `{"status":"ready","service":"wknowledge-web"}`。演练后已执行 `docker compose ... down --volumes --remove-orphans`，仅删除该临时项目数据。
- **Pi 安全边界不变**：本次部署基线携带 Pi Core、Skill 安装快照、Tool Registry/Policy Bridge、LLM Wiki Component、SQLite 本地档案/队列端口与 Pi 旁路，但 `WKNOWLEDGE_AGENT_LOOP=pi` 仍为显式旁路；S7 等价观察与旧循环清理门禁未完成前，生产默认保持内部循环，未借上线强行翻转。

## M7-12（切片）：PostgreSQL JobQueue 适配器——三实现同套件契约门禁成立

- 交付 `packages/core/src/postgres-job-queue.ts`：`createPostgresJobQueuePort`（JobQueue Port 的服务器档适配）——与 SQLite 适配器**同表结构同语义**（seq FIFO、SKIP LOCKED 独占 claim、ms 精度租约、幂等键、恢复/死信/取消），id 为 seq 生成列（`'job-' || lpad(seq,8,'0')`）避免跨实例计数器冲突。
- **设计决策（记录）**：Port 的服务器适配采用直连同义 SQL 而非包装 pg-boss——pg-boss 的 worker 回调模型与秒级租约无法满足本 Port 的 claim/lease/ms 契约；既有 pg-boss 任务（资源处理等）保持不变，M7-13 迁移期逐步移到本 Port。S4 门禁"PostgreSQL 与 SQLite 契约测试通过"自此**按字面成立**：同一 10 场景契约套件由内存基准、SQLite、PostgreSQL 三实现运行全绿。
- 测试：契约套件新增 PG 段（DATABASE_URL 门控、每场景 TRUNCATE 隔离——共享表与每场景新实例的内存/SQLite 不同,初版因场景间状态泄漏 7 失败,按证据加隔离修复;`require("vitest")` ESM 误用致文件未收集也已修正）。
- 全量门禁：typecheck/lint/format/build 均 0;`DATABASE_URL + --experimental-sqlite` 根测试 104 文件 / 593 条全部通过（三队列实现 + 三仓储场景全数运行）。

## M7-12/M5-16（切片）：Pi 会话 Repository 端口与 SQLite 适配器（runtime.sqlite）

- 交付 `packages/core/src/agent-session-repository-port.ts`：`AgentSessionRepository` 端口（createSession/appendMessage/beginRun/appendRunEvent/settleRun/listRunEvents/listSessionMessages）与内存语义基准——连续可重放事件序号、单次 settle、终态后拒绝追加、tool.requested 必带 tool+inputSummary、摘要 ≤300（与 PG `agent_run_event` 契约一致）。
- 交付 `sqlite-agent-session-repository.ts`：`node:sqlite` 实现（session/message/run/event 四表、外键与 CHECK 约束、UNIQUE(run_id, sequence)、文件或 `:memory:`),同 `SQLITE_RUNTIME_UNAVAILABLE` 门控。**Spec §7.1 `data/runtime.sqlite` 承载 Pi 会话/消息/事件/运行快照的本地档案落地**。
- 契约测试 9 条：4 场景共享套件内存+SQLite 双跑（会话消息顺序、连续事件序列、单次 settle、非法形状 fail-closed）+ 文件库跨重开持久化（重开后可读回会话消息——"Pi Session 丢失不损坏业务对象"的本地档案基础）。
- 全量门禁：typecheck/lint/format/build 均 0;`DATABASE_URL + --experimental-sqlite` 根测试 104 文件 / 583 条全部通过。
- 后置：PG 侧适配器把既有 agent-sessions 持久化映射到同一端口（S7 切默认时）、domain.sqlite 领域仓储、本地档案 Runtime Profile 装配（S8）。

## M5-14（第六切片）：安装/撤销组织管理 API

- 交付 `apps/web/app/api/settings/agent-skills/route.ts`：组织管理员 GET（安装历史列表）/POST（`{skillName, version?}` → 发现受管 `skills/installed` 目录 → `installAgentSkill` 固定 digest/分级快照,publisher=管理员）/DELETE（`{skillName}` 撤销）；输入经 contracts 新增 `agentSkillInstallRequestSchema`/`agentSkillRevokeRequestSchema` Zod 校验,鉴权与限流走既有 `settingsAdmin`/`settingsAdminMutation` 模式,目录中不存在的技能返回可操作 404。`installedSkillsRoot` 从 settings 导出复用。
- 领域行为（幂等安装/换版历史/撤销/resolve fail-closed）已由第五切片 DB 测试覆盖;路由为薄封装,遵循既有 settings 路由模式,真实浏览器设置页 UI 与 E2E 后置。
- 全量门禁：typecheck/lint/format/build 均 0;`DATABASE_URL + NODE_OPTIONS=--experimental-sqlite` 根测试 103 文件 / 574 条全部通过。

## M7-12/S7（切片）：SQLite 本地队列适配器、路由计数与旁路开关

- **SQLite JobQueue 落地（零新依赖）**：实测 `node:sqlite` 在本机 Node 23.2 以 `--experimental-sqlite` 可用（该 flag 对 22.5+/23.x 均有效，容器 22.23 兼容）；`packages/core/src/sqlite-job-queue.ts` 以动态导入实现 `JobQueuePort`（单表 + claim FIFO + RETURNING 原子更新,`SQLITE_RUNTIME_UNAVAILABLE` fail-closed）。上游实验性状态的取舍已注明：本地运行档案零第三方依赖优先,未来可在同一 Port 后替换驱动。
- **S4 队列契约门禁证据**：`job-queue-port.test.ts` 重构为共享 10 场景契约套件,内存基准与 SQLite 适配器**跑同一套件全绿**（20/20）;SQLite 套件按运行时可用性显式 skip（同步探测,不假通过）。
- **S7 计数与对比地基**：`agent-loop-routing.ts`——`recordAgentLoopRouting` 每轮对话写 `agent_loop.routing` 审计事件（旧/新路径调用计数有真相源,"两个观察窗口为 0" 门禁可查询）;`diffAgentTurnResults` 新旧整轮结构化对比（answer/evidenceIds/mode/insufficientEvidence）。路由 flag 改为 env 可覆盖（`WKNOWLEDGE_AGENT_LOOP=pi` 仅供旁路验证,默认内部路径）并在 begin 后记录路由。
- 测试 +13（共享契约套件重组 + SQLite 10 条 + routing 3 条）;调试记录：`claim.now` 契约是时间戳数字,内存与 SQLite 实现初版误当函数调用;routing 测试 fixture 缺真实用户 FK。

### 验证记录

```bash
DATABASE_URL=… NODE_OPTIONS=--experimental-sqlite pnpm test   # 103 files / 574 tests 全部通过
pnpm typecheck && pnpm lint && pnpm format:check && pnpm build  # 均 0
```

- 后置：SQLite Repository/事务适配器、pg-boss 适配器对齐同一契约套件、旁路等价验证执行（WKNOWLEDGE_AGENT_LOOP=pi + 真实 Provider + diffAgentTurnResults 报告）、S7 观察窗口与切默认。

## S7 前置（切片）：Web 会话路由 Pi 旁路接线（默认关闭）

- 交付 `apps/web/lib/pi-agent-turn.ts`（纯函数、全部单测）：`piTurnContexts`/`piSessionComponent`（binding→context/scope 映射与内部分支逐字段一致，scope→filter 的 wiki_page/resource_version/course 规则不变）；`piSessionPolicyBridge`（每次工具调用重核 `assertAgentSessionBindingsReadable`,撤权→原稳定码拒绝 + terminate,非码错误归一 `AGENT_SESSION_ACCESS_REVOKED`）；`knowledgeToolCallRecords`（事件流→`completeAgentSessionRun` 持久化记录,read 计数从 args/details JSON 解析、失败回退证据数）；`piToolStreamEvents`（toolCallId 配对映射 SSE 工具事件,文案与内部路径一致）。
- `runs/route.ts` 增加 `USE_PI_AGENT_LOOP = false` 旁路分支：与内部路径**共用同一** begin/abort/SSE/settle 脚手架与 `completeAgentSessionRun` 持久化契约（run 事件、工具记录、证据快照、助手消息全部复用既有函数）——切换只是翻转常量,不新增行为面。S7 观察门禁（旁路等价验证 + 旧调用计数两个窗口为 0）通过前保持内部路径默认。
- 编排器补齐多轮历史：`compactAgentConversation` 从 index 移入 knowledge-component 共享,`runPiKnowledgeTurn` 接受 `conversation`（有界历史作为不可信意图上下文进入 context.messages,assistant 历史以最小合法 AssistantMessage 形状构造,网关桥正常转换为 user/assistant 文本）。
- 单测 7 条（`apps/web/unit-tests/pi-agent-turn.test.ts`）：Policy 桥允许/撤权拒绝/非码归一、记录派生与坏 JSON 回退、SSE 映射配对与无工具流。
- 后置：旁路等价验证运行（翻常量跑真实 Provider 对比）、SSE 实时流式（当前回放式与内部路径一致）、S7 切默认与 M7-13 清理。

### 验证记录

```bash
pnpm exec vitest run apps/web/unit-tests/pi-agent-turn.test.ts
pnpm typecheck && pnpm lint && pnpm format:check && pnpm build   # 均 0
DATABASE_URL=… pnpm test    # 102 files / 561 tests 全部通过、零跳过
```

## M5-16/S7 前置（切片）：Pi 生产对话循环编排器 `runPiKnowledgeTurn`

- 交付 `packages/agent-runtime/src/pi-knowledge-loop.ts`：单个可生产接线的函数组装全部已验证构件——`KnowledgeComponent` 预建证据包（与内部路径一致，search 工具回放同一包，不二次查询 Wiki）、`createKnowledgeTools` + `createAgentToolRegistry` + Policy Bridge、`agentLoop` + `createGatewayStreamFn`（`purpose=wiki_query`）、`mapPiAgentEvent`/`piTerminalAgentCoreEvent` 折叠完整 `AgentCoreEvent` 流（供投影层）、`finalizeGroundedAnswer` 产出最终 `GroundedQueryResult`、记录网关包装器汇总 `modelCall`。
- 语义：无网关或空证据包 → 直接诚实降级（不进循环、modelCall=null）；终态唯一保证（loop 无终态时 `PI_AGENT_LOOP_ENDED_WITHOUT_TERMINAL` 兜底）；预算失败折叠 `run.failed(稳定码)` + extractive 降级；仅 `run.completed` 的最终文本有资格进入 GroundedAnswer 校验。
- 测试 4 条（`pi-knowledge-loop.test.ts`）：**整轮等价**——同脚本网关回合下 `runPiKnowledgeTurn.result` 与 `runBoundKnowledgeAgent(enableToolLoop)` 的 `GroundedQueryResult` 深度相等、事件序列精确匹配（started→search 请求/完成→read 请求/完成→delta→completed）；无网关/空包降级；预算失败终态与降级；Policy 拒绝在循环内生效（工具被 block 后模型仍完成有据回答）。
- 调试记录：`LOOP_MODEL` 常量需以 `AgentLoopConfig["model"]` 断言（`as const` 不满足 `Model<Api>` 泛型）；草稿中的死代码与运行时循环导入（value import 自 `./index`）已清理。
- 后置：Web 会话 Route 接入该编排器（begin→SSE 实时投影→settle）、投影器流式变体、S7 默认切换与 M7-13 旧路径清理。

### 验证记录

```bash
pnpm exec vitest run packages/agent-runtime/tests/pi-knowledge-loop.test.ts
pnpm typecheck && pnpm lint && pnpm format:check && pnpm build   # 均 0
DATABASE_URL=… pnpm test    # 101 files / 554 tests 全部通过、零跳过
```

## M5-16（切片）：Pi 运行事件投影持久化（SSE 重放真相源）

- 交付 `packages/core/src/agent-run-event-projection.ts`：`persistAgentCoreEventProjection` 把 Pi `AgentCoreEvent` 流折叠为 `agent_run_event` 行并落定 `agent_runs` 终态——SSE 重放（`getAgentRunEvents`/`Last-Event-ID`）继续以数据库为唯一真相源,Pi 内存事件丢失不影响重放（M5-16 验收语义）。
- 契约对齐（发现并修正三处与既有 DB 约束的偏差）：`beginAgentSessionRun` 已写入 `run.started`(seq 1),投影器跳过重复 started 并续接序号;`agent_run_event_shape_check` 要求 tool 行 **status NULL**、`run.failed` **不带** outputSummary;摘要上限是 **300**（非 ADR 文字的 2000,以 `agent_run_event_summary_size_check` 为准）。Fail-closed 校验:runId 一致、started 首位、tool 限 `knowledge.search/read`、requested 先于 completed 且无重复、终态唯一、未完成工具拒绝;run 非 running 拒绝(`AGENT_RUN_ALREADY_SETTLED`)、已投影拒绝、事务内无部分写入。
- DB 测试 7 条（`agent-run-event-projection.test.ts`）:投影+SSE 重放读取与序号、失败终态稳定码、四类非法流零部分写入、未知 run 拒绝。调试记录:初版误以为投影器自建 started 行、it.each 回调参数与局部变量重声明导致文件未被收集、三处 shape 违约——均按证据修正并原样记录。
- 既有测试基建修复:`office-parser-node-limits` 的 DOCX 预算用例生成 10001 段夹具本身 CPU 密集,全仓套件加入真实 DB 用例后在 15s 预算内超时(单跑 5.92s 通过、产品断言正常);该用例超时提高至 60s 并注释原因,非产品行为变更。

### 验证记录

```bash
DATABASE_URL=… pnpm test    # 100 files / 550 tests 全部通过、零跳过
pnpm test                   # 无 DB 模式同样通过
pnpm typecheck && pnpm lint && pnpm format:check && pnpm build   # 均 0
```

- 后置:Pi 循环生产接线把实时事件流式写入(begin→投影→settle 的流式变体)、SSE 端点切换读取路径、`persistAgentSessionTurn` 内部路径与投影器的收敛（S7 默认切换）。

## 本地测试数据库解锁与 M5-14（第五切片）：安装快照 DB 持久化与领域服务

- **环境解锁**：本机缓存有 `postgres:17-alpine` 镜像，启动一次性测试容器 `wknowledge-test-pg`（127.0.0.1:55432，`wknowledge_test` 库），完成全部 47 个迁移后以 `DATABASE_URL` 运行根 `pnpm test`——**99 文件 / 543 条全部通过、零跳过**（此前 103 条 DB 用例 skip）。无 DATABASE_URL 时回归 435 通过/108 跳过，两种模式均绿。DB 依赖切片自此可闭环验收。
- 交付 `packages/database` 迁移 `0047_agent_skill_installations`：`agent_skill_installation` 表（组织/技能名/版本/digest/来源格式与来源版本来源摘要/发布者/installedAt/enabled/executable），部分唯一索引 `(organization_id, skill_name) WHERE enabled` 固定"组织每技能单启用版本”，历史按组织+技能名+时间索引。
- 交付 `packages/core/src/agent-skill-installations.ts`：`installAgentSkill`（事务内 `FOR UPDATE` 锁定当前启用行——同 digest/版本/executable 幂等返回，换版本禁用旧行并插入新启用行，**历史保留不可变**）；`revokeAgentSkill`（仅一次生效）；`listOrganizationSkillInstallations`；`resolveOrganizationInstalledSkills`（启用快照 + agent-runtime `resolveInstalledAgentSkills` 组合——漂移/缺目录/重复均 fail-closed）。core 新增 `@wknowledge/agent-runtime` workspace 依赖（无环：agent-runtime 不依赖 core）。
- DB 测试 5 条（`agent-skill-installations.test.ts`）：幂等与换版历史、非法输入、撤销一次、resolve 漂移/缺目录 fail-closed、跨组织隔离。
- 调试记录（失败原样保留）：幂等重放首次失败，逐层定位（普通 select 正常→事务 select 正常→打印 SELECT 列清单）发现 **pgTable 定义与迁移 SQL 均漏写 `executable` 列**；补列后因本地库已应用旧 0047 报 `errorMissingColumn`，该迁移未提交未部署，直接修正 0047 并重建测试库（drop/create + 重跑迁移）通过。
- 后置：安装/撤销的 Web API 与设置页 UI、安装审批流（deny/ask/allow 会话接线）、catalog 目录与组织安装组合的 Worker/对话接线。

### 验证记录

```bash
DATABASE_URL=postgres://…@127.0.0.1:55432/wknowledge_test pnpm db:migrate
DATABASE_URL=… pnpm test        # 99 files / 543 tests 全部通过零跳过
pnpm test                       # 无 DB：435 passed / 108 skipped
pnpm typecheck && pnpm lint && pnpm format:check && pnpm build   # 均 0
```

## M6-15（首切片）：Learning Component 端口与语义基准

- 交付 `packages/core/src/learning-component-port.ts`：`LearningComponent` 端口（composePlanCandidate/confirmPlan/recordLearningEvent/currentProgress/snapshotReport）与 `createInMemoryLearningComponent` 内存语义基准——目标、计划、课程结构、学习事件、进度和报告归领域所有，模型/Skill 只能提议计划候选，事实指标不可被模型覆盖（S6 门禁）。
- 关键语义：候选单元必须带知识点与资料版本来源（空目标/空单元/重复 id 拒绝）；确认按候选幂等且每位学习者仅一个 active 计划；学习事件**仅追加**且带 sequence（unit_completed 必须指明单元、media_progressed 必须带非负 positionMs）；进度从事件日志确定性重建（完成集合、每单元最新媒体位置）；报告快照按计划一次性冻结——后续事件改变 currentProgress 但不重写已发布报告。
- 测试 `packages/core/tests/learning-component-port.test.ts` 5 条：候选校验、确认幂等与单 active 计划、事件追加与形状校验、进度确定性重建、报告冻结不被后续事件重写。
- 调试记录：实现方法名初版与接口不一致（`composeCandidate` ≠ `composePlanCandidate`）导致 5 条 TypeError，改名后通过。
- 后置：数据库适配器对照既有 learning-plans/learning-progress-report 领域行为、Learning Tool/Skill 经 Tool Registry 组合、报告 Worker 图片路径接驳。

### 验证记录

```bash
pnpm exec vitest run packages/core/tests/learning-component-port.test.ts
pnpm typecheck
pnpm lint
pnpm format:check
pnpm test
pnpm build
```

- 根 `pnpm test`：73 files passed、25 skipped，435 tests passed、103 skipped（本轮 +5）。typecheck、lint、format:check、build 均以退出码 0 通过。

## M6-14（首切片）：Assessment Component 端口与语义基准

- 交付 `packages/core/src/assessment-component-port.ts`：`AssessmentComponent` 端口（composeCandidate/publish/submit/gradeObjective/requestReview）与 `createInMemoryAssessmentComponent` 内存语义基准——正式题卷、Attempt、Grade 均为不可变快照，不依赖 Pi Session（S5 门禁）。
- 关键语义：Skill 只能经 `composeCandidate` 产出候选（题目必须带 knowledgePoint/resourceVersion/SourceLocator，exact_response 项必须带答案键、free_response 项必须带量表，否则 `ASSESSMENT_*` 拒绝）；`publish` 按 candidateId 幂等冻结快照；每卷每位学习者仅一次完整作答（缺失/重复/未知项均拒绝）；`gradeObjective` 对 exact_response 按冻结答案键确定性评分、free_response 标记 `pending-review` 不冒充评分，Grade 幂等不可变；`requestReview` 只接受 free_response 项。
- 测试 `packages/core/tests/assessment-component-port.test.ts` 9 条：候选校验矩阵（空/重复 id/缺答案键/缺量表）、发布幂等与未知候选、单次完整作答矩阵、确定性评分与自由题延后、复核路由。
- 后置：数据库适配器对照同一契约（复用既有 assessments 领域表）、考试 Skill 经 Tool Registry 组合、正式状态与既有 M6-07/M6-08 行为对齐验证。

### 验证记录

```bash
pnpm exec vitest run packages/core/tests/assessment-component-port.test.ts
pnpm typecheck
pnpm lint
pnpm format:check
pnpm test
pnpm build
```

- 根 `pnpm test`：72 files passed、25 skipped，430 tests passed、103 skipped（本轮 +9）。typecheck、lint、format:check、build 均以退出码 0 通过。

## M7-12（首切片）：JobQueue Port 契约与内存本地队列适配器

- 交付 `packages/core/src/job-queue-port.ts`：`JobQueuePort` 契约（enqueue 幂等键/claim 独占租约/complete 终态幂等/fail 有界重试与死信/cancel 仅 queued/recoverExpiredLeases/list）与 `createInMemoryJobQueuePort` 内存适配器——为未来 SQLite 档案固定语义基准，服务器档按同一契约映射 pg-boss；域组件后续只经此端口入队。
- 语义要点：同 idempotencyKey 的 open/completed 任务不重复提交（failed/cancelled 后可重新入队为新任务）；claim 计入 attempts 且租约内独占；崩溃恢复把过期 active 重回 queued、耗尽次数的死信为 failed（`JOB_LEASE_EXPIRED`）；终态不可再变更，非 active 的 complete/fail 以 `JOB_NOT_ACTIVE` 拒绝。
- 测试 `packages/core/tests/job-queue-port.test.ts` 10 条：幂等合并与终态重放、双 worker 独占、完成幂等与终态保护、仅 queued 可取消、重试耗尽死信、过期租约恢复与死信分流、未过期租约不动。时间经注入 clock 控制。
- 后置：SQLite 持久适配器（需驱动选型与供应链核验，S4 后续）、Repository/Transaction Port、pg-boss 适配器对照同一契约测试、Runtime Profile 装配。

### 验证记录

```bash
pnpm exec vitest run packages/core/tests/job-queue-port.test.ts
pnpm typecheck
pnpm lint
pnpm format:check
pnpm test
pnpm build
```

- 根 `pnpm test`：71 files passed、25 skipped，421 tests passed、103 skipped（本轮 +10）。typecheck、lint、format:check、build 均以退出码 0 通过。

## M3-13（第三切片）：Pi 最终回答 GroundedAnswer 契约

- `extractiveFallback` 从内部循环移入 `knowledge-component.ts` 共享；新增 `finalizeGroundedAnswer(modelOutput, evidence)`：模型输出只有在合法 JSON、`mode=generated` 且引用仅含当前 bundle 证据（`groundedQueryResultSchema` 内建 `GROUNDED_ANSWER_EVIDENCE_UNKNOWN`/`INSUFFICIENT_ANSWER_CANNOT_CITE_EVIDENCE`/`GROUNDED_ANSWER_EVIDENCE_REQUIRED` 校验）时才成为答案；任一不过或输出缺失一律诚实降级 `extractive_fallback`，绝不外显无据回答。内部循环与 Pi 路径现共享同一最终回答契约。
- 测试 +6（knowledge-tools 文件 10 条）：合法回答通过；非 JSON、伪造 evidenceId、insufficient 却引用、非 generated mode、缺失输出全部按契约降级；空 bundle 保持显式拒答。
- 至此 S3 退出门禁“EvidenceBundle、SourceLocator、拒答与范围结果一致”的三层自动化证据齐备：组件 search 逐字段等价（第一层）、Pi 与内部路径工具结果逐字节相等（第二层）、最终回答同一 GroundedAnswer 契约（第三层）。

### 验证记录

```bash
pnpm exec vitest run packages/agent-runtime/tests/
pnpm typecheck
pnpm lint
pnpm format:check
pnpm test
pnpm build
```

- agent-runtime 包内 10 文件 192 条全部通过。根 `pnpm test`：70 files passed、25 skipped，411 tests passed、103 skipped。typecheck、lint、format:check、build 均以退出码 0 通过。
- 后置：生产 AgentSession/SSE 切换、默认切换与旧路径删除（S7/M7-13）。

## M3-13（第二切片）：knowledge.list/search/read 注册为 Pi Tool 与新旧问答旁路对比

- 交付 `packages/agent-runtime/src/knowledge-tools.ts`：`createKnowledgeTools(component, { question, signal })` 产出 `knowledge.list`/`knowledge.search`/`knowledge.read` 三个 `RegisteredAgentTool`——search 参数为空（查询固定为当前用户问题，与既有工具契约一致）、read 限 1–10 个 evidenceId、list 返回 scope 摘要；`source.open` 保持组件/UI 操作，刻意不暴露给模型（来源区面向用户，非模型能力）。工具输出复用从内部循环提出的 `searchToolOutput`/`readToolOutput`（移至 knowledge-component.ts 共享，index 转出），保证两条路径负载形状逐字节可比。
- **旁路对比测试**（S3 退出门禁第二层）`tests/knowledge-tools.test.ts` 4 条：真实临时 Wiki 空间 + 真实 `agentLoop` + mock 网关——Pi 路径（Registry + 组件 + 网关桥 + allow-all Policy）与内部路径（`runBoundKnowledgeAgent` `enableToolLoop`）在同脚本回合下，模型收到的 `knowledge.search` 与 `knowledge.read` 工具结果内容**逐字节相等**；read-before-search 以可恢复错误工具结果呈现（run 正常收尾）；非法问题在工具构造处拒绝。
- 调试记录（失败原样保留）：内部循环工具模式强制 search+read 都完成后才接受最终答案（`MODEL_TOOL_CALL_REQUIRED` 兜底会无工具消息重发），且最终回答必须是合法 generated JSON——对比脚本据此改为三段回合与 JSON 答案；网关捕获器初版跨 payload 重复累计工具消息，改为只取最终 payload。
- 组件等价（第一层）+ 工具 I/O 旁路对比（第二层）共同构成“EvidenceBundle、SourceLocator、拒答与范围结果一致”的自动化证据；生产 SSE/AgentSession 接线与默认切换仍后置。

### 验证记录

```bash
pnpm exec vitest run packages/agent-runtime/tests/
pnpm typecheck
pnpm lint
pnpm format:check
pnpm test
pnpm build
```

- agent-runtime 包内 10 文件 186 条全部通过（本轮 +4）。根 `pnpm test`：70 files passed、25 skipped，405 tests passed、103 skipped。typecheck、lint、format:check、build 均以退出码 0 通过。
- 后置：Pi 路径最终回答的 GroundedAnswer JSON 契约与引用校验接线、生产 AgentSession/SSE 切换、`runBoundKnowledgeAgent` 旧路径替换（M7-13 清理门禁）。

## M3-13（首切片）：LLM Wiki Component 端口与共享有界检索等价基线

- 交付 `packages/agent-runtime/src/knowledge-component.ts`（升级 Spec §4.1）：`KnowledgeComponent` 端口——`listScopes`（只暴露 binding/kind/spaceId/label，无宿主路径）、`search`（scope 固定于构造自已验证 Binding，入参仅问题，与现有工具契约一致“查询固定为当前用户问题”）、`read`（只返回同轮已过滤 EvidenceBundle 的摘录，≤10 个 id，未知 id 剔除，搜索前读取拒绝 `KNOWLEDGE_READ_BEFORE_SEARCH`）、`openSource`（只允许打开调用方已收到证据所属来源：evidenceId∈bundle、sourceIndex 合法、scope 按 `spaceId__` 前缀匹配后委派注入 opener）。
- `createBoundKnowledgeComponent` 依赖注入：`resolveSpaceRoot`（宿主解析存储根，路径不进端口）与 `openSource`（真实预览鉴权留在既有 Web 边界）、`assertReadable`（撤权重核）。scope 校验与现有 `AGENT_CONTEXT_INVALID` 语义一致（1–8、唯一 bindingId、非空 label）。
- **零行为变化重构**：`runBoundKnowledgeAgent` 的有界检索合并逻辑原样提取为共享 `searchBoundKnowledge`（多空间并行检索、`spaceId__evidenceId` 合并、去重、截断 10、embeddingCalls=0），Agent 改为调用共享函数；既有 16 条 bound agent 回归即等价基线。
- 等价测试（S3 退出门禁第一层）`tests/knowledge-component.test.ts` 11 条：真实临时 Wiki 空间下，组件 `search` 与 `runBoundKnowledgeAgent` 的 EvidenceBundle **逐字段相等**——多空间合并、resourceVersion 过滤（排除版本内容不出现）、无命中诚实拒答（items 空且两路径一致）；read/openSource/listScopes 边界矩阵与 scope 校验。Evidence 摘录文本断言对照 bundle 本身（wiki `answerExcerpt` 按命中词截取，不以原始节点内容断言）。

### 验证记录

```bash
pnpm exec vitest run packages/agent-runtime/tests/
pnpm typecheck
pnpm lint
pnpm format:check
pnpm test
pnpm build
```

- agent-runtime 包内 9 文件 182 条全部通过（本轮 +11）。根 `pnpm test`：69 files passed、25 skipped，401 tests passed、103 skipped。typecheck、lint、format:check、build 均以退出码 0 通过。
- 后置：`knowledge.list/search/read/source.open` 注册为 Pi Tool（Tool Registry + Policy Bridge 组合）、组件承载的问答旁路对比（S3 完整门禁）、`runBoundKnowledgeAgent` 旧路径在等价门禁后的替换与 M7-13 清理。

## M5-14（第四切片）：安装快照契约、内容 digest 与固定版本解析

- `@wknowledge/contracts` 新增 `agentSkillInstallationSnapshotSchema`（Spec §6.2“平台安装时生成不可变记录”）：id/organizationId/skillName/version/`digest`（安装时固定的内容摘要）/sourceFormat（`skill.json` 迁移或 `agent-skills-directory` 直装）/sourceVersion+sourceDigest（迁移来源保留，对接转换器 `conversion` 记录）/publisher/installedAt/enabled/executable。数据库持久化属后续 core/database 切片。
- `computeAgentSkillCatalogDigest`：对发现层实际观察到的字节做确定性 SHA-256——排序后的顶层条目列表 + `SKILL.md` + manifest（如存在）。**条目列表纳入哈希**：即使两个文件一字未动，静默新增 `scripts/` 目录或删除 references 也会改变 digest，堵住“目录内容漂移但文件不变”的绕过面。更深层文件内容由执行准入固定（现有 sandbox 已验程序文件 digest），catalog 不做全量遍历，边界在代码注释中明确。
- `resolveInstalledAgentSkills({ snapshots, skills })`（tenant-scoped ResourceLoader 解析核心，ADR 0004 决策 4）：快照 `enabled=false` → 撤权，过滤不报错；已安装技能从受管根消失 → `AGENT_SKILL_SNAPSHOT_UNRESOLVED` fail-closed（安装物消失是篡改信号，不静默降级）；内容 digest 与快照不符 → `AGENT_SKILL_SNAPSHOT_DRIFT` fail-closed；目录中无快照的技能对组织**不可见**；同名重复快照 → `AGENT_SKILL_SNAPSHOT_INVALID`（组织固定一个版本，非“先扫描到者获胜”）。
- 测试 +9（catalog 文件 24 条）：digest 跨发现稳定、SKILL.md 变更/条目增删三种漂移均改变 digest；解析矩阵——已装且启用按 pin 服务、disabled 撤权过滤、目录缺失与 digest 漂移 fail-closed、无快照不可见、重复快照非法。

### 验证记录

```bash
pnpm exec vitest run packages/agent-runtime/tests/
pnpm typecheck
pnpm lint
pnpm format:check
pnpm test
pnpm build
```

- agent-runtime 包内 8 文件 171 条全部通过（本轮 +9）。根 `pnpm test`：68 files passed、25 skipped，390 tests passed、103 skipped。typecheck、lint、format:check、build 均以退出码 0 通过。
- 后置：快照的 DB 表与安装/撤销领域服务（core/database）、安装 UI 与审批流（deny/ask/allow 会话接线）、catalog 与对话会话 ResourceLoader 组合、内置目录落盘迁移。

## M5-15（首切片）：Tool Registry 与 Policy Bridge

- 交付 `packages/agent-runtime/src/agent-tool-registry.ts`（ADR 0005 §3 冻结映射的实现）：领域组件以 `RegisteredAgentTool`（名称 `^[a-z][a-z0-9.-]{0,99}$`、JSON Schema、`risk` 分级、`requiredScope`、handler）注册;`toPiTools()` 是 Pi 循环的唯一工具来源——只含已注册工具,绝不注入 Bash/Read/Write 等默认工具;重复注册与非法定义以 `AGENT_TOOL_DUPLICATE`/`AGENT_TOOL_DEFINITION_INVALID` 拒绝。
- `AgentToolPolicyBridge` 是 pi-free 的 Wknowledge 契约(权限/审批/预算在前,裁剪/来源挂接/审计在后);`policyHooks()` 适配到 Pi `AgentLoopConfig.beforeToolCall/afterToolCall`:拒绝 → `block + 稳定码`(可带 `terminate`);Policy 抛错(权限库/审计库宕机)→ fail-closed `AGENT_TOOL_POLICY_FAILED` 且 `terminate: true`,不让未经检查的结果通过;未知工具防御性 `AGENT_TOOL_UNKNOWN`;after 钩子异常时以错误结果替换原内容而非放行原文。
- 语义核对(基于 pi 0.84.2 `agent-loop.js` 源码):Pi 对伪造参数的路径是 `validateToolArguments` 抛错 → `prepareToolCall` catch → immediate error tool result——handler 不执行、run 继续;标量宽容强转(如数字→字符串)是上游“宽容修复”契约(ADR 0005 §3 原文),失败关闭门禁覆盖不可修复形状。未知工具名由 Pi 先行返回 "Tool not found" 错误结果,注册表钩子再作防御。
- 测试 `tests/agent-tool-registry.test.ts` 18 条:单元(注册校验/重复/映射、决策矩阵、override 与 fail-closed)+ 真实 `agentLoop` + 网关桥集成——撤权 block 不执行 handler、伪造参数(缺必填/结构错误)在 Schema 校验处拒绝且 run 继续、注入文本经 after 裁剪替换(原文不进入模型上下文)、拒绝带 `terminate` 停止后续模型调用(网关只被调用 1 次)+ 模块守卫(仅 pi 类型导入,无 node:/env/数据库)。
- 调试记录(失败原样保留):伪造参数用例最初用 `{"query":123}`,实际被上游宽容强转为 `"123"` 后执行——按 ADR 契约改为不可修复形状(缺必填、对象误作字符串);两处 `exactOptionalPropertyTypes` 类型错误(signal 传参与 override 合并)已修复复跑。

### 验证记录

```bash
pnpm exec vitest run packages/agent-runtime/tests/
pnpm typecheck
pnpm lint
pnpm format:check
pnpm test
pnpm build
```

- agent-runtime 包内 8 文件 162 条全部通过（本轮 +18）。根 `pnpm test`：68 files passed、25 skipped，381 tests passed、103 skipped。typecheck、lint、format:check、build 均以退出码 0 通过。
- 后置：`knowledge.search/read` 等生产工具从 `runBoundKnowledgeAgent` 迁入注册表并接真实权限/审批/预算(需 S2 后续与生产接线)、Agent Skill catalog 与对话会话的组合、`runBoundKnowledgeAgent` 旧循环在等价门禁后的替换与 M7-13 清理。
