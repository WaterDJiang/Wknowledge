# 2026-08-17 实施记录

## M5-14（第三切片）：skill.json → SKILL.md + 执行契约转换器与 digest 映射

- 交付 `convertLegacySkillManifest`（`packages/agent-runtime/src/agent-skills.ts`）：旧 `skill.json`（contracts `skillManifestSchema`）一次性转换为新标准布局，满足升级 Spec §8“迁移工具必须保留原始 digest 和版本映射”。
- 映射规则：动态 entrypoint（`typescript-json-cli`/`python-json-cli`）生成 `wknowledge.manifest.json`（runtime 原样、entry 为固定程序文件 `run.mjs`/`run.py`、io/能力/权限/限额复制、artifacts 空）；内置 Worker 派发（`index.ts`）与内部分发（`worker:*`）**不**伪造执行契约——转为 instruction-only，其执行接线按清理表移交 Tool Registry 与 Assessment/Learning 组件（M5-15/M6-14/M6-15），杜绝“转换即获得动态执行权”的提权路径。
- 生成的 `SKILL.md`：frontmatter `name`（旧 id）/`description`（旧 description），正文为诚实迁移脚手架——显式声明“自动迁移生成、非作者工作流说明”，并列出来源版本与 digest 的 provenance 与结构化契约摘要；不虚构工作流指令。
- Fail-closed 校验：旧 id 违反新 name 契约（如前导数字）、description 含换行或超 1024，均 `AGENT_SKILL_CONVERSION_INVALID` 拒绝。返回 `conversion` 记录（sourceFormat/sourceVersion/sourceDigest/sourceEntrypoint/executable）供安装快照持久化。
- 测试 +8（`tests/agent-skills.test.ts` 46 条）：动态转换含往返 `classifyAgentSkill` 分级、内置/worker entrypoint 转 instruction-only、生成 SKILL.md 经标准解析器往返（name/description/provenance）、3 条非法源拒绝、真实 7 个内置 `skills/builtin/*/skill.json` 夹具全部转换通过（均 instruction-only、digest 保留）。

### 验证记录

```bash
pnpm exec vitest run packages/agent-runtime/tests/
pnpm typecheck
pnpm lint
pnpm format:check
pnpm test
pnpm build
```

- agent-runtime 包内 7 文件 144 条全部通过（本轮 +8）。根 `pnpm test`：67 files passed、25 skipped，363 tests passed、103 skipped。typecheck、lint、format:check、build 均以退出码 0 通过。
- 后置：内置 Skill 目录实际落盘迁移与 `skills/builtin` 布局切换（待安装快照与 Tool Registry 接线后执行，属 M7-13 清理门禁）、组织安装快照与固定版本解析、对话/Worker 接线。

## M5-14（第二切片）：受管 Skill 目录发现层（显式根、符号链接拒绝、同名冲突 fail-closed）

- 交付 ADR 0004 决策 4 的发现载体 `discoverAgentSkillCatalog`（`packages/agent-runtime/src/agent-skill-catalog.ts`）：每次调用只读一个显式传入的受管根目录——无默认路径、绝不扫描服务器主目录；根级常规文件（如 README）忽略，子目录一律必须是合法 Skill（缺 `SKILL.md`、`SKILL.md` 为目录/符号链接均 `AGENT_SKILL_DIRECTORY_INVALID`/`_SYMLINK_REJECTED` 拒绝），复用首切片 `classifyAgentSkill` 完成 instruction-only/executable 分级，跨目录同名 `SKILL.md` name 直接 `AGENT_SKILL_NAME_CONFLICT`（不“先扫描到者获胜”；组织固定版本解析属安装快照切片）。
- 预算与编码：根条目 ≤1024、Skill 数 ≤256、Skill 内条目 ≤1024、`SKILL.md` ≤1 MiB、manifest ≤256 KiB，超限 `AGENT_SKILL_CATALOG_LIMIT`/`_FILE_TOO_LARGE`；条目名含 `/`、`\`、NUL、`.`/`..` 防御性拒绝 `AGENT_SKILL_ENTRY_UNSAFE`。
- fs 以 `AgentSkillFsAdapter` 注入（listEntryNames/statEntry/readFileContent），发现编排本身不触碰真实 fs；`createNodeAgentSkillFsAdapter` 提供 Node 实现——`lstat` 检测符号链接、`fstat` 后按预算读取、`TextDecoder(fatal)` 拒绝非法 UTF-8（`AGENT_SKILL_CONTENT_INVALID_UTF8`）。
- 测试 `tests/agent-skill-catalog.test.ts` 15 条：内存 fake 适配器矩阵（发现/空根/忽略文件、10 条拒绝用例、数量上限）+ Node 适配器真实临时目录链路（符号链接→同名→正常发现→超限→非法 UTF-8）+ 模块守卫（导入仅限 `node:fs/promises`、`node:path`、本地与 contracts；禁网络/env/数据库）。
- 调试记录（失败原样保留）：首轮 fake 适配器把根目录名误作一层路径导致 14 条 ENOENT（修复 lookup 跳过首段）；真实目录用例因字母序处理顺序使同名冲突先于符号链接触发（给 linked-skill 独立名字后通过）。

### 验证记录

```bash
pnpm exec vitest run packages/agent-runtime/tests/
pnpm typecheck
pnpm lint
pnpm format:check
pnpm test
pnpm build
```

- agent-runtime 包内 7 文件 136 条全部通过（本轮 +15）。根 `pnpm test`：67 files passed、25 skipped，355 tests passed、103 skipped。typecheck、lint、format:check、build 均以退出码 0 通过。
- 后置：组织安装快照与固定版本解析、skill.json → `SKILL.md`+manifest 转换器与 digest 映射、对话页/Worker 接线与 Tool Registry/Policy Bridge（S2 主线）。

## M5-14（首切片）：标准 SKILL.md 解析、instruction-only/executable 分级与执行契约校验

- 经连续开发指令按计划推进（原 S2 排期 09-07 起；并行工作流 B 本就把“Agent Skills 解析”列为 S1 期准备）。范围：升级 Spec §6.1/§6.2 的解析与分级层，不含目录发现、安装快照与 skill.json 转换器。
- `@wknowledge/contracts` 新增 `agentSkillManifestSchema`（`wknowledge.manifest.json`）：仅声明 runtime（`typescript-json-cli`/`python-json-cli`）、entry、输入/输出 Schema、能力、权限、限额与 artifacts；身份/来源/版本/digest 归安装快照（后续切片）。entry/artifacts 强制安全相对路径（拒绝绝对路径、反斜杠、`..`/`.` 段、NUL），权限与限额复用既有 `skillManifestSchema` 形状。
- `packages/agent-runtime/src/agent-skills.ts`（新）：`parseSkillMarkdown` 解析标准 `---` frontmatter——受限单行标量语法（键 `[A-Za-z][A-Za-z0-9_-]*`，值可整对去引号），嵌套/列表/块标量/重复键/空键值等以 `AGENT_SKILL_FRONTMATTER_UNSUPPORTED`/`_INVALID` fail-closed；name 限 `^[a-z][a-z0-9-]{0,63}$`，description ≤1024，license ≤200，未知标量键忽略（生态前向兼容），正文非空。不为单一解析引入 YAML 依赖；无法无歧义表示的形态宁可拒绝。
- `classifyAgentSkill`：目录条目含 `wknowledge.manifest.json` 时按 schema 校验为 executable（失败即 `AGENT_SKILL_MANIFEST_INVALID`）；无 manifest 一律 instruction-only，`scripts/` 目录或顶层脚本文件只置 `undeclaredExecutableContent` 标记——按 Spec“未声明执行契约的脚本不可执行”，可导入但永不进入执行准入。
- 纯函数设计：输入仅为字符串与条目名列表，无 fs/网络/数据库/密钥；模块级守卫测试断言无 `node:` 导入、无 `process.env`、导入仅限 `@wknowledge/contracts` 与本地相对路径。tenant-scoped 目录发现（不扫描主目录）与安装快照属后续切片。
- 测试 `tests/agent-skills.test.ts` 38 条：frontmatter 合法矩阵（含引号冒号、CRLF、未知键忽略）与 18 条非法拒绝（缺 frontmatter/未闭合/名称与描述边界/嵌套/列表/块标量/重复键/空正文等）、分级矩阵（docs-only、scripts 标记、合法 executable、10 条 manifest 拒绝：空/非 JSON/未知 runtime/路径穿越/绝对路径/反斜杠/artifacts 穿越/限额越界/权限枚举）、纯度守卫。

### 验证记录

```bash
pnpm exec vitest run packages/agent-runtime/tests/
pnpm typecheck
pnpm lint
pnpm format:check
pnpm test
pnpm build
```

- agent-runtime 包内 6 文件 121 条全部通过（本轮 +38）。根 `pnpm test`：66 files passed、25 skipped，340 tests passed、103 skipped。typecheck、lint、format:check、build 均以真实退出码 0 通过（首轮 typecheck 曾因解构 `string | undefined` 失败，已修复并复跑）。
- 后置：skill.json → `SKILL.md`+manifest 转换器与 digest 映射、受管目录 ResourceLoader 与安装快照、组织发现冲突按固定版本解析（S2 后续切片）；内置 Skill 迁移与旧 `skill.json` 清理属 M7-13。

## M5-13/M5-16（S1 第三切片）：取消/多轮/逐事件等价闭合 S1 自动化门禁

- 目标：闭合 ADR 0005 §6 第 2 项“真实合成 Tool Loop（含取消、错误、多轮）等价”。错误路径（预算超限稳定码）已由第二切片覆盖，本切片补取消、多轮与逐事件等价断言。
- 修复生产缺陷（取消链路）：`createGatewayStreamFn` 此前忽略 pi 每次调用传入的 `options.signal`（只用构造期 signal），且 Gateway 因中止拒绝（真实网关预检抛 `MODEL_PROVIDER_CANCELLED`）时被折叠为 `run.failed`，违反 ADR 0005“AbortSignal → run.stopped”映射。现 per-call signal 与构造期 signal 经 `AbortSignal.any` 合并传入 Gateway；拒绝且 signal 已中止时推 `reason:"aborted"` 错误事件，经 stopReason `aborted` 折叠为 `run.stopped(cancelled)`。
- 抽取共享折叠纯函数：`PiAgentCoreAdapter` 私有 mapEvent/terminalEvent 提取为导出的 `mapPiAgentEvent`/`piTerminalAgentCoreEvent`，Adapter 复用；等价测试用同一折叠函数处理真实环原始事件，保证“真实网关驱动环 ↔ 脚本契约”比较使用完全相同的映射（无第二套映射漂移）。
- 新增集成回归（`pi-gateway-integration.test.ts` 4→7 条）：①search→read→回答 真实环折叠事件与 `PiAgentCoreAdapter` 脚本回放 `toEqual` 逐事件相等（传递性等于内部 Adapter）；②工具环间取消（search 工具内 abort）→ 折叠终态唯一 `run.stopped`、仅 1 次网关调用、read 回合与最终回答的脚本响应未被消费、无后续工具执行；③第二用户回合以新 run 携带累积历史，payload 角色序列 `system/user/assistant/tool/assistant/user` 每条消息恰一次，且两回合折叠事件分别与对应脚本回放相等——多轮层面回归消息复制缺陷类别。
- 新增桥接单元回归（`model-gateway-bridge.test.ts` 8→10 条）：per-call signal 优先且与构造期 signal 合并（abort 任一则合并 signal 中止）；中止拒绝折叠为 `reason:"aborted"`、无 errorMessage 失败码。

### 验证记录

```bash
pnpm exec vitest run packages/agent-runtime/tests/
pnpm typecheck
pnpm lint
pnpm format:check
pnpm test
pnpm build
```

- agent-runtime 包内 5 文件 83 条全部通过。根 `pnpm test`：65 files passed、25 skipped，302 tests passed、103 skipped。typecheck、lint、format:check、build 均通过。
- S1 自动化退出门禁（ADR 0005 §6）已闭合：①既有 20 条轨迹（8 有效等价 + 12 无效拒绝）；②真实合成 Tool Loop 含取消、错误、多轮的等价；③源码级权限守卫（导入白名单、`node:` 敏感模块、`process.env`、数据库/队列负向断言）。
- 后置：生产 AgentSession/SSE 接线（属 S2+ 接线与 S7 默认切换）、真实 Provider 运维验收（需管理员配置受管 Provider）、`beforeToolCall/afterToolCall` Policy Bridge 与 Tool Registry（S2）、SQLite 投影（S4）。

## M5-13/M5-16（S1 第二切片）：网关桥接入真实 Pi Loop 与消息复制缺陷修复

- 承接首切片遗留的 `pi-gateway-integration.test.ts`（上次会话中断时未验证）：首轮运行 4 条中 1 条失败，网关 payload 中用户消息出现两次。
- 根因（生产缺陷）：`PiAgentCoreAdapter` 把 `context.messages` 同时作为 `agentLoop` 首参与 `context` 传入；pi-agent-core 0.84.2 的 `runAgentLoop` 会把 prompts 追加到 `context.messages` 之后，streamFn 收到的上下文每条消息重复两遍。合成流Fn忽略上下文，因此首切片 41 条轨迹等价测试未暴露该缺陷。
- 修复：改用 `agentLoopContinue(context, ...)`（语义即"context 已含用户消息"）。`message_start/end`、`turn_start` 均被 mapEvent 忽略，映射事件流不变；`agent_end.messages` 仅含新消息，`terminalEvent` 只找最后一条 assistant 消息，行为不变。既有 33 条 pi-adapter 测试与 8 条新旧 Adapter 等价轨迹全部保持通过。
- 集成测试同步修正误用：两个用例改为规范接线——用户回合只作为 `agentLoop` prompt 传入，历史 `messages: []`，并留注释说明重复传入会在网关 payload 中复制消息。
- 集成回归内容：`knowledge.search → knowledge.read → 回答` 的真实 `agentLoop` 工具环经 `createGatewayStreamFn` 走 mock Model Gateway；断言工具顺序、最终 assistant 消息、网关 3 次调用的 payload 形状（system + user + assistant tool_calls + tool results + tool 定义）、预算超限稳定错误码 `MODEL_BUDGET_EXCEEDED` 结束为 `agent_end`；以及 pi-adapter/网关桥源码导入与 `node:fs/net/child_process/http/crypto/dns`、`process.env`、`getDatabase/pg-boss` 权限守卫。

### 验证记录

```bash
pnpm exec vitest run packages/agent-runtime/tests/
pnpm typecheck
pnpm lint
pnpm format:check
pnpm test
pnpm build
```

- agent-runtime 包内 5 文件 78 条全部通过（首切片 74 + 集成 4）。根 `pnpm test`：65 files passed、25 skipped，297 tests passed、103 skipped。typecheck、lint、format:check（修复一处集成测试格式后）与 build 均通过。
- 未完成/后置：生产 AgentSession/SSE 接线、真实 Provider 轨迹、`beforeToolCall/afterToolCall` Policy Bridge 与 Tool Registry（S2）、SQLite 投影（S4）。

## M7-01/M7-02/M7-11：生产容器启动时 Corepack 联网解析失败

- GitHub Actions 运行 `31952801549` 的 `validate` 和生产 preflight 均已通过；部署在 `migrate` 容器退出码 1 后停止。
- 服务器只读命令 `docker logs --tail 200 wknowledge-migrate-1` 返回 `getaddrinfo EAI_AGAIN registry.npmjs.org`。Corepack 在解析 `pnpm` 时尝试查询 npm registry，数据库迁移命令尚未执行。
- 生产镜像改为构建时直接安装固定 `pnpm@10.29.3`，继续使用 `pnpm-lock.yaml --frozen-lockfile`，使 `migrate`、`web` 和 `worker` 启动不依赖 Corepack 用户缓存或运行时外网。

### 验证计划

```bash
pnpm exec vitest run packages/core/tests/docker-runtime-dependency-retries.test.ts
pnpm format:check
bash -n deploy/alicloud/deploy.sh
POSTGRES_PASSWORD=compose-test-password WKNOWLEDGE_CREDENTIAL_KEY=... WKNOWLEDGE_HTTP_HOST_PORT=127.0.0.1:13000 WKNOWLEDGE_RELEASE_VERSION=ci-validate docker compose config --quiet
```

- 推送只表示触发新部署；必须取得新 Actions `success` 与服务器回环 ready 证据后，才可继续配置宝塔反代和 HTTPS。

## M7-01/M7-02/M7-11：修复历史检出残留的 Nginx 配置权限

- 服务器 firewalld 的 Docker bridge 未加入 `docker` zone，导致容器访问 PostgreSQL 与 DNS 均报 `EHOSTUNREACH`；已将 `br-b641ce596b1d` 即时并永久加入 `docker` zone，临时容器验证返回 `postgres:5432 - accepting connections`。
- GitHub Actions 重跑 `31957867814` 后，`migrate` 成功，PostgreSQL、Web 和 Worker 均健康；gateway 日志原样报错 `open() "/etc/nginx/nginx.conf" failed (13: Permission denied)`。
- 服务器只读检查确认 SELinux 为 `Disabled`，挂载源 `/opt/wknowledge/app/deploy/nginx/wknowledge.conf` 为 `0600`，而 Git 只记录普通文件 `100644`。旧检出受 `umask 077` 影响，后续 `checkout --force` 不会恢复组/其他用户读位。
- 回归测试先原样失败：`expected deploy.sh to contain chmod 644 "$app_dir/deploy/nginx/wknowledge.conf"`。部署脚本现于每次 checkout/clean 后仅把公开 Nginx 配置恢复为 `0644`，不改变 `runtime.env` 的 `0600`。

### 验证计划

```bash
pnpm exec vitest run packages/core/tests/docker-runtime-dependency-retries.test.ts
pnpm format:check
bash -n deploy/alicloud/deploy.sh
POSTGRES_PASSWORD=compose-test-password WKNOWLEDGE_CREDENTIAL_KEY=... WKNOWLEDGE_HTTP_HOST_PORT=127.0.0.1:13000 WKNOWLEDGE_RELEASE_VERSION=ci-validate docker compose config --quiet
```

- 推送后仍须等待 Actions `success`、gateway healthy 和服务器回环 readiness，不能把提交或触发视为部署完成。

## M1-02/M1-09：公开邮箱验证码试用注册

- 增加公开注册页、发送六位邮箱验证码和验证注册 API。`WKNOWLEDGE_ALLOW_SIGNUP=true` 时，任意有效邮箱均可注册；未实现邮箱或域名白名单、管理员审批、到期或付费限制。
- 验证码只保存 HMAC 哈希，十分钟有效且单次消费；发送和验证均使用既有同源防护与持久化公开 mutation 限流。验证成功后，在一个事务中创建独立试用组织、owner 成员关系和“我的知识库”空间，再写入 HttpOnly 会话。
- Web 支持 Resend 或 SMTP（SMTP 优先）；发信密钥只传入 Web 容器。生产 preflight 在公开注册开启而 `from` 或发信凭据缺失时失败，避免上线后无法收码。

### 验证记录

```bash
pnpm exec vitest run packages/core/tests/deployment-preflight-cli.test.ts packages/core/tests/compose-public-signup.test.ts apps/web/unit-tests/signup-email.test.ts packages/core/tests/trial-signup.test.ts
pnpm typecheck
pnpm lint
pnpm test
pnpm format:check
POSTGRES_PASSWORD=ci-compose-password WKNOWLEDGE_CREDENTIAL_KEY=... WKNOWLEDGE_HTTP_HOST_PORT=127.0.0.1:13000 WKNOWLEDGE_RELEASE_VERSION=ci-validate WKNOWLEDGE_ALLOW_SIGNUP=true WKNOWLEDGE_RESEND_API_KEY=re_test WKNOWLEDGE_EMAIL_FROM='Wknowledge <noreply@example.com>' docker compose config --quiet
bash -n deploy/alicloud/deploy.sh
```

- 聚焦测试 7 passed、1 skipped；数据库集成用例因本机未配置 `DATABASE_URL` 跳过。根 `pnpm test` 在授予本机回环监听权限后通过：61 files passed、25 skipped，250 tests passed、103 skipped。
- 尚未提交、推送或改动云端 `runtime.env`；需提供 Resend API Key 或 SMTP 凭据，并经明确授权后才能开启公开注册部署。

## M1-02：注册验证码表单与登录跳转修复

- 复现：验证码发送成功后，React 在条件表单切换时复用了首个 input DOM 节点，导致验证码输入带入邮箱；深色系统主题下认证面板背景变深，但标签和输入边界未同步调整，显示名称和密码输入难以辨认。
- 修复：邮箱请求页和验证码页使用带 key 的独立表单；验证码、显示名称和密码改为受控状态，验证码只接收六位数字。所有认证输入均改为有边界和底色的 48px 控件；深色主题显式提供高对比文字、边框、输入底色与自动填充颜色。
- 登录与注册成功后直接替换到 `/workspace/resources`，不再经过 `/workspace` 的额外重定向；生产站点只读检查确认 `https://knowledge.wattter.cn/login` 使用 HTTPS，Secure 会话 Cookie 可被浏览器保存。

### 验证记录

```bash
pnpm typecheck
pnpm lint
pnpm format:check
pnpm build
pnpm test
pnpm exec playwright test apps/web/tests/auth-flow.spec.ts
pnpm test:e2e
```

- 新增 Playwright：390px 深色验证码页在模拟成功发送后验证码为空、显示名称/密码可见，且输入文字与底色可读；模拟密码登录后 URL 为 `/workspace/resources`。聚焦 E2E 2 passed。
- `pnpm test`：61 files passed、25 skipped，250 tests passed、103 skipped。类型检查、Lint、格式检查和构建通过。
- `pnpm test:e2e`：22 passed、1 failed。原命令中既有“access management APIs require authentication”期望无效邀请为 `409`，实际为 `503`；`enforcePublicMutation` 在调用 PostgreSQL 持久化限流失败时返回 `REQUEST_GUARD_UNAVAILABLE`。这是 E2E 运行时数据库/限流依赖不可用，并非本次认证 UI 改动；未静默重试或篡改该既有用例。

## M1-09：HTTPS 反向代理同源校验修复

- 线上只读复现：`POST https://knowledge.wattter.cn/api/auth/login` 携带 `Origin: https://knowledge.wattter.cn` 返回 `403 CSRF_ORIGIN_DENIED`。
- 根因：宝塔 HTTPS 配置正确传入 `Host` 和 `X-Forwarded-Proto: https`，但应用只使用容器内部请求 URL 的 `http` 协议构造期望 Origin，导致 HTTPS 浏览器写请求被误判为跨站。
- 修复：同源校验优先采用明确的 `http`/`https` 转发协议，并继续以当前 Host 匹配 Origin；新增单元回归覆盖 HTTPS 反代通过与真实跨站拒绝。
- 验证：目标 Vitest 4 passed；根 `pnpm test` 为 62 files passed、25 skipped，252 tests passed、103 skipped；`pnpm format:check`、`pnpm lint`、`pnpm typecheck`、`pnpm build` 与认证 Playwright 2 passed 均通过。

## M7-01/M7-11：TLS 反向代理协议透传修复

- 线上复验仍返回 `403 CSRF_ORIGIN_DENIED`。链路为“宝塔 HTTPS → Docker gateway HTTP → Web”：宝塔正确传递 `X-Forwarded-Proto: https`，但 gateway 用自身 `$scheme`（HTTP）覆盖该头，因此应用无法识别外部 HTTPS。
- 修复：gateway 仅在收到明确 `http` 或 `https` 的外层协议时透传该值，否则使用自身协议；Web 继续严格比较 Origin 与 Host。
- 先补静态回归并确认原始配置失败：`packages/core/tests/compose-upload-gateway.test.ts` 报缺少 `map $http_x_forwarded_proto $wknowledge_client_scheme`。修复后该回归与同源单元测试 3 passed；`docker compose config --quiet` 通过；`nginx -t` 通过。

## M3-13/M5-13 至 M5-16/M6-14/M6-15/M7-12 至 M7-14：Pi 核心组件升级规划

- 用户批准目标结构：Pi 负责对话循环、会话事件、上下文与 Tool 调度；LLM Wiki、Assessment、Learning 以外部领域组件接入；Wknowledge 继续掌握权限、来源、审计、模型网关和 Sandbox。
- Skill 采用 Agent Skills 兼容目录：`SKILL.md` 作为人类与 Agent 可读入口，`wknowledge.manifest.json` 保存可执行、版本、能力和策略扩展；不再把仓库内 `skill.json` 作为长期唯一格式。
- Runtime Profile 分为 `local` 与 `server`：本地使用 SQLite、本地文件队列和 LocalBlob，安装后无需 PostgreSQL/pg-boss 即可运行；线上继续使用 PostgreSQL、pg-boss 和既有 Blob 适配。
- 新增升级 Spec、ADR 0004 与 2026-08-18 至 2026-12-11 的九阶段排期。M5 先迁移核心与 Skills，M3/M6 再组件化 Wiki、Assessment、Learning，最后完成 SQLite、默认切换、旧实现删除和安装验收。
- 清理范围已显式纳入 M7-13：内部生产 Loop、`runBoundKnowledgeAgent`、内置 `skill.json`、旧发现器、Worker 学习生成分支、Skill Runtime 重复发现职责、业务直连数据库/pg-boss 和已失效的“Pi 仅候选”文档表述。
- PostgreSQL/pg-boss Server Adapter、历史迁移、审计/来源/学习记录与不可变原始证据不删除；任何旧路径只在替代路径通过等价、安全和数据迁移门禁后清理。
- 当前状态仅为“设计完成”：Pi 依赖未安装，运行时代码、Schema 和部署配置均未改动。
- 验证：`pnpm format:check` 通过；`git diff --check` 无空白错误；新 Spec、ADR、计划、主计划、交付状态和索引文件均存在。

## M5-13（S0）：Pi 版本锁定、供应链档案与 Node 基线统一

- 版本调研（npm registry 实测）：`@earendil-works/pi-agent-core` 自 `0.78.0` 起全部声明 Node `>=22.19.0`，仅 `legacy-node20` 标签（`0.74.2`）支持更低版本；锁定"兼容 22.12 的旧版"不可取。决策：采用最新 `0.84.2` 精确 pin（无 `^`），并统一 Node 基线到 22.19+。计划文档中上游 `0.83.0` 引用已更新为锁定事实。
- Node 基线统一：根 `package.json` engines `>=22.12.0` -> `>=22.19.0`；`deploy/Dockerfile` 基镜像 `node:22.12-bookworm-slim` -> `node:22.23.2-bookworm-slim`（Node 22 LTS 最新，2026-07-28）；`AGENTS.md` 开发环境行同步。本机 Node v23.2.0 满足新基线。
- 依赖安装：`packages/agent-runtime/package.json` 增加 `"@earendil-works/pi-agent-core": "0.84.2"`；`pnpm install` 成功，lockfile 记录 sha512 integrity。pnpm 10 拦截 `@google/genai`、`protobufjs` 构建脚本，决策为保持不批准（provider SDK 附属，fail-closed）。
- 供应链档案：新增 `doc/ref/pi-core-supply-chain-v1.md`——锁定版本/integrity/来源、依赖树快照（94 个唯一包）、许可证审计（Apache-2.0 44、MIT 36、BSD-3-Clause 12、0BSD 1、ISC 1，无 copyleft、无未知）、安装期脚本审计（三个 Pi 包无 install 钩子）、风险与控制（pi-ai provider SDK 由 Model Gateway 唯一路由隔离；遥测 NOOP）、升级须重跑档案的门禁。
- 映射冻结：新增 `doc/design/0005-pi-core-mapping-frozen.md`——基于 0.84.2 实际 `dist/*.d.ts` 固化 Pi `AgentEvent` -> `AgentCoreEvent`/`AgentRunEvent`/SSE、Tool Registry -> `AgentTool`（含 `beforeToolCall/afterToolCall` Policy Bridge）、Pi 运行态与业务 `AgentSession` 持久化边界、`streamFn` Model Gateway Bridge。S1 Adapter 必须按表实现。
- 状态同步：`doc/plan/INDEX.md`（Pi 计划 设计完成 -> 执行）、`doc/plan/delivery-status-v1.md`（新增 `M5-13（S0）` 开发中行与本轮开发条目）、`doc/INDEX.md`、`doc/ref/INDEX.md`、`doc/design/INDEX.md`。
- 验证：`pnpm format:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test`（62 files、252 tests passed、103 skipped）、`pnpm build` 全部通过。
- 未完成/后置：Docker 镜像真实构建（本机既往 Docker Hub 拉取超时，未纳入本次验收）；`PiAgentCoreAdapter` 实现、20 条轨迹与真实合成 Tool Loop 等价、安全反例门禁均属 S1（08-24 起）。

## M5-13/M5-16（S1 首切片）：PiAgentCoreAdapter 与 Model Gateway Bridge

- 经用户确认提前启动 S1（原排期 08-24 起）。计划要点：既有 20 条轨迹夹具是等价基线，Pi Adapter 以同一份脚本驱动真实 `agentLoop`，与内部 Adapter 输出逐事件相等。
- `agent-core.ts`：提取纯函数 `validateAgentCoreScript`（同规则：runId/toolCallId/toolName/summary 校验、顺序、终态唯一），`InternalAgentCoreAdapter` 改为复用；既有 20 条测试未改、全部通过。
- `pi-adapter.ts`（新）：`PiAgentCoreAdapter` 把校验后的脚本编译为分段合成 pi-ai 流（文本增量、toolCall、done/toolUse、error）与顺序 `AgentTool`，运行真实 `agentLoop`，按 ADR 0005 映射：`agent_start`->`run.started`、`text_delta`->`assistant.delta`、`tool_execution_start/end`->`tool.requested/completed`、abort->`run.stopped`、错误->`run.failed`（稳定码）、`agent_end`->`run.completed`。构造零参数，不注册任何 Pi 默认工具，不接触数据库/文件/网络/密钥。
- `model-gateway-bridge.ts`（新）：`createGatewayStreamFn` 以 Model Gateway 为唯一 Provider 路由；`contextToGatewayPayload` 只接受 user/assistant/toolResult 三种消息形态，未知形态 fail-closed `PI_CONTEXT_UNSUPPORTED`；Gateway 返回文本或 tool_calls 时合成 `AssistantMessageEventStream`；失败映射稳定错误码（`MODEL_BUDGET_EXCEEDED` 等原样保留，非码错误归一 `MODEL_GATEWAY_FAILED`）；不注入 `getApiKey`。
- 测试：`tests/pi-adapter.test.ts` 33 条（8 条有效轨迹新旧 Adapter 逐事件等价、8 条终态唯一、12 条非法轨迹拒绝、停止语义 3 条、结构检查）与 `tests/model-gateway-bridge.test.ts` 8 条全部通过；包内 4 文件 74 条通过。
- 调试记录（失败原样保留）：首轮 5 条失败，根因一为合成工具 Schema `additionalProperties:false` 未声明 `inputSummary` 导致 loop 以错误结果替代（修复 Schema）；根因二为编译器预开空 segment 使“立即失败”脚本先正常完成（改为惰性开段）；根因三为测试期望遗漏 `type:"toolCall"` 字段。
- 会话异常（安全相关，待复核）：探索期间多次工具输出（Read/grep/git show 回显）显示 `runBoundKnowledgeAgent` 中存在“`evidenceIds` 被映射为常量占位符”的代码，且同一文件的不同读取结果自相矛盾。经不依赖字符串回显的字节级核验（hex 比对 HEAD 第 592 行为 `` `$glm-5.3_common` `` 的正确逐条映射）及既有 13 条回归通过确认：真实代码无缺陷，相关“占位符”内容为被篡改/伪造的工具输出。未据此修改任何生产代码。该输出篡改迹象建议在安全侧独立复核（检查本机 hooks/输出过滤层）。
- 未完成/后置：生产 AgentSession/SSE 接线、真实 Provider 合成 Tool Loop 等价、`beforeToolCall/afterToolCall` Policy Bridge（S2）、ResourceLoader 与 Tool Registry（S2）、SQLite 投影（S4）。
