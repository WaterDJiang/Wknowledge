# Wknowledge 项目文档索引

进入项目后先读本文件，再按需读取各分类的 `INDEX.md` 和 1–2 个相关分片。

## 文档分类

| 分类    | 用途                         | 入口                               |
| ------- | ---------------------------- | ---------------------------------- |
| spec/   | 产品需求、领域契约与验收标准 | [spec/INDEX.md](spec/INDEX.md)     |
| plan/   | 分阶段实施计划               | [plan/INDEX.md](plan/INDEX.md)     |
| log/    | 执行记录，最新在上           | [log/INDEX.md](log/INDEX.md)       |
| design/ | 总体设计、分域设计与 ADR     | [design/INDEX.md](design/INDEX.md) |
| ref/    | 外部调研与参考资料           | [ref/INDEX.md](ref/INDEX.md)       |

## 当前焦点

- 最新治理单元：`M0-04` 根 Harness、文件职责与文档闭环已完成 13 项质量核验。
- 当前状态：M0–M3、M4 多模态、M5 与 M6 学习闭环持续开发；PDF、PPTX、表格和图片已完成可重复解析与精确回源切片，生成型 Skill、模型主观评分/长期掌握推断和 M7 仍为开发中或骨架，见 [交付状态](plan/delivery-status-v1.md)。
- 最新验证单元：`M4-05/M5-08` 已将受管 ASR 音频准入从 WAV 扩展到 WAV、MP3、M4A；三者均须通过扩展名、MIME、容器签名和健康/启用/策略相容 Provider 检查。无真实 Provider 不创建注定失败的任务，真实转写验收仍后置。
- 最新验证单元：`M4-06/M5-08` 已允许管理员声明受管 `vision` Provider；Worker 只把受限 JPEG 关键帧交给与空间数据策略相容的 Provider，并将合格 JSON 写为同版本、同时间点、明确标记 `ai_completed` 的画面描述。无 Provider、无效模型响应或策略不相容时安全跳过，完整视频理解和真实 Provider/浏览器验收仍后置。
- 最新开发单元：`M7-05` 已定义维护窗口快照、SHA-256 清单校验与拒绝覆盖的恢复边界；Compose 增加隔离的备份/恢复卷和 PostgreSQL 17 Client 运行时。CLI 回归和 Compose 配置已通过，但 Docker Hub 拉取 `postgres:17-bookworm` 超时，真实镜像构建与完整恢复演练仍后置。
- 最新开发单元：`M7-02` 已接入无泄露 preflight，并让本地 Web/Worker/Playwright 通过根 `.env` 继承缺失配置：它检查生产数据库 URL、非默认数据库密码、发布版本、32-byte 凭据主密钥、受管存储根隔离与容量下限；Compose 现将同一凭据主密钥传入 Web/Worker。真实 Docker 运行与 Secret/KMS 注入仍待验证。
- 最新开发单元：`M7-03` 已加入 Drizzle 迁移历史只读检查，按 journal/SQL SHA-256 要求数据库记录为连续前缀。本地数据库被准确识别出 0025、0035、0040 三条历史 hash 漂移；检查保持 fail-closed，不能通过改写历史或自动迁移掩盖。
- 最新开发单元：`M7-04` 已加入空间级 Wiki v1 Schema 清单、历史无清单 v1 双读、维护窗口 staging/Lint/原子发布迁移与 Compose 运维入口。现有抽样空间检查为 `pending_manifest`；Web/Worker 仍在运行，未触发写入迁移。
- 最新开发单元：`M7-06` 已增加数据库 readiness 与管理员组织运营快照：系统设置汇总处理积压/超时、模型健康、24 小时模型失败率和容量阈值，只返回计数与稳定告警码；Worker 心跳继续开发。
- 最新验证单元：`M7-07` 已为受管模型调用加入组织、Provider 与用户的原子滚动日预算、Gateway 拒绝/Provider fallback、设置页有效额度和 429/SSE/Worker 稳定失败码；实际 token/费用计量、在线预算编辑、外部网关限流和告警仍后置。
- 最新验证单元：`M7-08` 已提供管理员受控审计导出；首切片只导出本组织事件元数据，并明确不自动删除审计记录。
- 最新验证单元：`M7-09` 已提供 localhost/staging 安全边界的 Web/database readiness 负载工具与 100 请求/10 并发本机基线；`format`、`lint`、`typecheck`、305 项单测、构建和 21 项 E2E 均通过。部门级混合工作负载验收仍后置。
- 当前开发单元：`M7-10` 原始扫描确认的 5 个 high 均已完成对应修复：`M7-10-A` Agent 会话撤权读取、`B` 组织任务控制面、`C` Provider endpoint/凭据、`D` membership 级成员禁用、`E` 动态 Skill 地址空间限制、`F` Compose 必填口令与内部数据库端口。复扫发现的 Agent/Worker membership 暂停绕过、Provider IPv4-mapped IPv6 非公网 DNS 分类、公开限速伪造转发头、XLSX/CSV 整表物化、Worker 聚合资源、分片上传无界请求体读取及 DOCX/PPTX 节点无界增长已由 `M7-10-G/H/I/J/K/L/M1/M3` 收敛；`M7-10-M4` 已为 Docker APT 依赖层增加有限重试，`M7-10-M5` 已隔离 Worker 临时盘。直传 multipart 的 9 MiB 网关配置已进入 Compose，但完整 Web 运行时验收尚未完成。当前完整复扫尚未完成，不能据此宣称 high 已清零；Linux Sandbox、恶意文件与容器资源演练仍待完成，medium 按运行时可用性顺序整改。
- 最新验证单元：`M7-10-M6` 已为 Node 文本、Python PDF 和 Worker ASR 加入读取前大小/结构预算，避免全量解码、无页数遍历和完整字节复制；保持来源定位和既有 PDF/音频合法输入行为。对应安全 finding 仍待修复后复扫确认。
- 最新验证单元：`M7-10-M7` 已将编译正文、PDF 页图、视频关键帧和学习报告 Artifact 纳入组织容量账本；Worker 在写入前按增长量预留，额度不足时不替换既有 compiled 或写入报告 Blob。历史目录回填与修复后复扫仍后置。
- 最新验证单元：`M7-10-M8` 已补齐组织成员暂停后的学习资料、计划确认/事件以及 Agent Session/Binding 新写入重核；既有运行时撤权与跨组织隔离不变。
- 最新验证单元：`M7-10-M9` 已让来源预览只读取用户请求的受权字节段，避免大文件预览先被全量读入服务端内存；S3 Adapter 的等价 Range GET 仍后置。
- 当前安全整改：2026-08-14 标准复扫确认 2 个 high、4 个 medium；`M7-10-R1` 至 `R6` 已进入排期。运行中 Agent 撤权、环境变量 cloud Provider 固定传输和学习来源授权是对话与生成型学习 Skill 的启用门，见 [安全复扫整改](plan/security-rescan-remediation-m7-10-v1.md)。
- 当前执行范围：继续完成权限、任务韧性、Wiki 浏览/审核、黄金集和多模态；基于既有 M4 来源回源的确定性学习闭环已包括候选练习、个人正式测评、基础客观 Grade、私有报告导出及满足准入媒体的固定时间定位播放，但不提前接入第三方生成型 Skill 或主观模型评分。
- 最新验证单元：`M3-01/M3-03/M3-10` 真实 64 页 PDF 经新 knowledge 版本由 Worker 解析为 8 个独立 topic，查询命中全部章节且 Embedding 为 0；PDF 原文件预览/页码高亮仍属于 M4。
- 最新开发单元：`M2-01` 已实现 4 MiB 分片上传会话、续传状态、完整 SHA-256 校验和控制面最终化入队；Worker 创建不可变版本后接续资料处理进度，真实浏览器大文件验收待补。
- 最新验证单元：`M6-06` 已允许已完成课程单元生成确定性、有来源的候选练习；候选题明确不自动评分、不改变掌握度或报告，真实练习 Skill 与审核/评分仍后置。
- 既有验证单元：`M6-08` 为候选题加入不可变作答快照；自由作答保持人工复核，基础客观回顾题的确定性 Grade 已由最新 M6-08/M6-09/M6-13 单元接续。
- 最新验证单元：`M6-13` 已将当前课程的确定性指标固化为不可变报告快照；Worker 基于同一 JSON 生成私有 PNG/PDF，支持 Outbox、执行租约恢复、本人历史快照浏览与受管读取。模型解释和分享仍后置。
- 最新验证单元：`M6-07` 已将学习者确认的候选练习冻结为独立正式题卷；题面、答案键、量表、知识点、资源版本和来源均随题卷/作答快照保存，每题仅一次提交，客观题确定性评分，独立页面不与日常练习混放。生成型出题、组织审核、主观复核和掌握度仍后置。
- 最新验证单元：`M6-08` 已提供组织管理员的自由作答人工复核队列；日常练习和正式测评均按冻结题面/量表评分，Grade、学习事件与组织审计同事务追加，评分不覆盖原始回答或调用模型。模型建议分、二次复核和掌握度仍后置。
- 最新验证单元：`M6-10` 已将学习首页收敛为概览和下一步入口；内容与计划、固定原文课程、练习与测评、报告和正式测评均有独立路由，继续复用既有权限、来源和版本快照契约。单元级辅导与真实登录移动端验收仍后置。
- 最新验证单元：`M4-06` 已允许 MP4 直传/分片上传；Worker 在本地提取媒体结构、流清单、文本型内嵌字幕 cue 和受限 JPEG 关键帧。存在策略相容 ASR Provider 时，第一音轨临时转为 WAV 后生成 `video` 时间定位转写；无音轨、Provider 不可用或提取超限时保留容器/字幕/关键帧结果。关键帧 OCR 和视觉理解仍关闭。
- 最新验证单元：`M4-02` 已将 PPTX 文本 Shape、表格单元格与备注解析为不可变 `slide + shapeId` 来源节点；来源页只读取当前已发布历史版本的精确 Shape 或单页派生文字，不能回退其他幻灯片或版本。原版式渲染、图片 OCR、图表语义和真实登录点击流仍后置。
- 最新验证单元：`M4-04` 已将 PNG/JPEG/WebP 的本地 OCR 行解析为不可变图片 bbox 来源节点；原图预览只叠加精确 OCR 区域并标记派生文字，不能回退其他区域或把无文字图片伪装成已理解。视觉理解、手写/表格专项识别、模型调用和真实登录点击流仍后置。
- 最新验证单元：`M4-01/M4-08/M4-09` 已将 PDF 原生文字块解析为不可变 `page + point bbox` 来源节点，并用 Worker 派生同版本页图显示精确覆盖层；空白/扫描页面不伪造 OCR 内容。扫描 OCR、复杂阅读顺序和分类型定位评测仍后置。
- 最新部分验证单元：`M4-10` 已建立 PDF、PPTX、表格、图片 OCR、音频 ASR 和视频音轨转写的人工预期合成定位基线与 `pnpm eval:locators` JSON 报告；它会严格拒绝资源版本、页码、Shape、范围、bbox 和时间段错误，不替代真实资料盲测、PDF 95% 人工定位验收或音视频误差评测。
- 最新验证单元：`M4-03` 已将 CSV/XLSX 解析为不可变工作表范围节点；来源页只读取已发布的指定 `sheet + range` 文本和公式摘要，原始工作簿仍受权下载。在线网格、公式计算和高级格式语义仍后置。
- 最新验证单元：`M6-09` 已基于每题最近一次确定性 Grade 呈现当前错题回顾；再次答对即从清单移除，历史错误证据不删除，仍不推断掌握度或错因。
- 最新验证单元：`M5-03/M5-06/M5-07` Worker 已安全执行固定内置 `wiki-lint`，并能分派受管动态 CLI 的内部 Run；受管目录的安全发现、组织启停与会话可执行性已接通。两者均在执行前重核版本、组织启停、范围和成员授权，只保存脱敏摘要。动态输入只含 Binding 元数据，模型、网络、Shell、原始文件读取与浏览器安装仍关闭。
- 最新验证单元：`M6-11` 已将客观与人工 Grade 固化为知识点的不可变评分证据快照，并投影到当前学习报告；报告/导出只保留稳定 ID 与指标，不含原文名或来源 URI。它不使用模型推断，不把待复核或历史课程数据伪装为长期掌握度。
- 最新开发单元：`M4-05` WAV 上传与 Worker 现在共享 ASR Provider 可用性重核；转写阶段可见，Provider 在排队后停用、不健康或与数据策略不相容时不会读取音频或调用模型。真实 Provider/WAV 验收待管理员配置受管服务后执行。
- 最新验证单元：`M2-01/M2-11` 已补齐 Local Blob 每次临时/不可变写入的本地卷容量预检、分片会话创建前的完整申报大小预检和脱敏 `507`；告警和 S3 容量适配仍待实现。
- 当前实施单元：`M2-01/M2-04/M2-06/M2-11` 已补齐组织 1 GiB 默认配额、唯一 Blob 计量、并发预留、上传 507、`ENOSPC`/`EDQUOT` 容量错误归一、Worker 任务失败/重试、最终化失败安全终态、本地 Blob 只读巡检与过期 `open/failed` 分片会话临时 Blob 清理。
- 最新验证单元：`M2-03/M3-03` 既有资料可从当前版本创建复用不可变 Blob 的新编译模式版本，重新进入 Worker 与 Wiki 编译；旧版本、来源和已发布页面保持不变。
- 最新完成单元：`M2-03/M2-10` 小文件替换会创建同一资料的新不可变版本；资料页可展开安全的历史版本元数据与任务状态。
- 最新验证单元：`M2-02` 已在 Blob/数据库/队列前校验文件名、扩展/MIME、PDF/Office 签名、UTF-8 和 Office ZIP 安全边界；PDF/Office 语义深检与上传一致性后续项未开始。
- 最新完成单元：`M5-03/M5-08/M5-10` 模型 Provider 与内置 Skill 管理设置、加密凭据、健康检查和运行时接线。
- 最新开发单元：`M5-06/M5-07` 已建立版本、范围和审批快照固定的 `SkillRun`、独立 Outbox 与 `skill.run` 队列投递；Worker 对固定 `wiki-lint` 走内置 Handler，对受管 `skills/installed/{skillId}` 的动态 CLI 走 Linux fail-closed Sandbox，管理端安装/生成型 Skill 仍未开放。
- 最新开发单元：`M6-01/M6-02/M6-03/M6-05/M6-12` 已实现用户自述画像、选材、计划确认、固定版本原文和追加学习事件；生成型练习、正式测评、主观评分和掌握度仍后置。
- 最新验证单元：`M6-04` 已在计划确认时固化 Course、Module、Unit 和来源锚定的结构重点；学习页按课程结构打开固定版本原文。它不调用模型或 Skill，练习、评分和报告仍后置。
- 最新完成单元：`M3-11/M5-01` 查询、候选、引用和模型调用记录持久化，以及设置页只读审计视图。
- 最新完成单元：`M3-12` 黄金集评估框架与 6 份受控资料/12 问题试点基线。
- M3-12 并行准备：正式 development/blind 评估已具备授权/去敏/人工标注 Review Manifest 与 `--formal` 机器门禁；仍需资料授权后导入 100 份真实资料、50+ 问题执行盲测。
- 最新完成单元：`M3-05` 人工批准、重新打开、审计和已审核页面重编译保护首切片。
- 最新完成单元：`M3-05b/M3-08b` 待发布版本快照、逐行 diff 与受控接受/拒绝完成真实编辑者浏览器验收。
- 最新完成单元：`M3-04` 冲突事实并列、来源快照与“保留并列”人工裁决完成真实编辑者浏览器验收。
- 最新完成单元：`M3-06` 空间级发布锁、发布 manifest、短租约心跳与 Worker 启动恢复已验证。
- 最新完成单元：`M2-04/M2-06/M2-11` Outbox、任务取消/恢复、死信队列、执行租约、compiled checkpoint 与完整 Worker 强杀/restart 恢复已完成自动化验证。
- 当前实施单元：`M1-03/M1-05` 本地邀请、禁用与空间成员管理已完成自动化验证。
- 最新完成单元：`M1-09` Cookie 同源防护、PostgreSQL 持久化限流与错误脱敏已通过自动化门禁。
- 最新验证切片：`M2-06/M2-11` Worker 执行租约、启动恢复与过期任务安全重新入队，以及 `M3-06` Wiki 空间发布租约与恢复已通过数据库/队列边界回归。
- 最新浏览器验收：`M3-04/M3-05b/M3-08b` 编辑者在隔离空间完成候选接受、候选拒绝与并列冲突裁决；来源面板和状态同步正确。
- 最新规划变更：M5 后续采用类 Codex/Claude 的 `knowledge.search/read` ToolCall、脱敏审计和受管知识虚拟路径；M6 固定“内容选择 → 确认计划 → 原文学习 → 生成候选/作答/评分 → 可回查报告 JSON → Worker 图片”闭环。Pi 仅通过 Adapter Spike 评估，OpenCode 仅借鉴 Skill/权限模式，Claude Code 仅对照公开 UX/Hook；硬约束、页面/API、排期和验收切片已收录于 [扩展计划](plan/agent-learning-expansion-v1.md)。
- 最新验证单元：`M5-12` 新会话创建页现在按空间、Wiki 页面、资料版本或已确认课程显式组装范围清单；服务端只写入已校验 Binding，指定子范围不再隐式扩大为整空间，且无效目标不留下 session/binding/audit 半成品。真实 Provider 的已登录对话 E2E 仍后置。
- 最新验证单元：`M4-06` 视频关键帧可在 Worker 本地 OCR 画面文字，并以同一历史视频版本、关键帧时间和像素 bbox 入库；面板仅展示“该帧 OCR 文字”，不冒充视频理解。真实 MP4 的已登录人工预览与画面理解仍后置。
- 最新计划优化：`M5/M6` 已将“受管多轮对话 + 指定知识 Binding/虚拟路径 + 对话 Skill”和“选材 → 计划候选 → 固定原文 → 练习/评分 → 报告图片”排入同一执行队列；Pi 为 Adapter 候选，OpenCode/Claude Code 仅按已审查边界借鉴，不以完整 Coding Agent 方式嵌入。
- 最新验证单元：`M5-03/M5-06` 已将完整空间 Worker Skill 与页面/资料版本/Course 子范围强制区分；子范围无法创建会被整空间读取的 Skill 审批或队列任务。
- 最新开发单元：`M6-03` 已将合格 `plan-compose` 的结构化输出暂存为仅本人可读的候选产物；只有当前学习者已完成的 Run、显式选材、可覆盖 Binding 和一致 SourceLocator 才能一次性创建带 version/digest 追溯的 draft，确认前不创建 active Plan 或 Course。真实模型调用、受管安装与页面触发仍后置。
- 最新验证单元：`M6-03` 学习内容页已显示本人已完成 Run 的计划候选及固定来源，并可用新目标一次性创建草稿；页面不能改写 Unit/资料版本或重复物化候选，真实生成触发仍后置。
- 已完成基础单元：`M5-08/M6-03` 已建立 `LearningGenerationRequest` 私有输入表，并把计划目标、精确 ResourceVersion Binding、SkillRun 和 Outbox 划分为受控请求层。
- 最新开发单元：`M5-08/M6-03` 已接通 `plan-compose` Worker 内置 Provider handler 和学习页受控入口；本地模型可读取受权编译摘要，云端脱敏策略仅收到去标识资料结构。`practice-generate`、真实 Provider 运维验收与运行进度显示仍后置。
- 最新开发单元：`M5-08/M6-06` 已建立 `practice-generate` 的已完成 Unit 私有请求与精确 Course Binding；Provider Worker、候选页面触发和题目候选验收继续开发。
- 最新开发单元：`M5-08/M6-06` 已接通 `practice-generate` Worker、受控页面触发与候选确认；真实 Provider 与候选 Run 的浏览器进度验收仍后置。
- 最新开发单元：`M6-06` 已为 `practice-generate` 建立与 SkillRun 一对一的受限候选产物和一次性题集物化 API；只有当前 active Course 的精确 Binding、已完成 Unit、固定 KnowledgePoint/ResourceVersion/SourceLocator 与正确题型/量表才可创建 AI candidate。真实安装、模型调用和学习页触发仍后置。
- 最新计划收敛：对话、学习与开源适配的产品化边界已单列为 [M5/M6 产品化 Spec](spec/agent-skill-learning-production-enablement-m5-m6-v1.md)。当前优先完成“已登录的显式 Binding 对话验收 → Linux Sandbox 实测 → 生成型计划/练习候选”，不将已有确定性骨架误称为生成型 Agent/学习闭环完成。
- 最新完成单元：`M4-08/M4-09` 已实现按 SourceLocator 授权读取历史原件、Range 响应和独立来源预览页面；真实 V2 PDF 成功跳转第 9 页，Range、桌面/390px 与控制台均已验证，PDF/图片/文本预览及 Office 下载降级通过自动化门禁。
- 最新开发单元：`M4-07` 已实现本地受控 `ffprobe` 媒体探测 CLI；合成 WAV/MP4 与受管 LocalBlob 的 Worker 解析选择均可输出时间段 SourceLocator 和 ParserOutput。MP4 上传、文本型内嵌字幕、关键帧和条件化第一音轨转写已由 M4-06 接续；关键帧 OCR 和视频理解仍受各自策略门禁约束。
- 最新验证单元：`M5-08/M4-05` 已为受管 OpenAI-compatible Provider 增加显式 `speech_to_text` 能力、加密配置、multipart 调用、`verbose_json` 分段和空间数据策略路由。合法分段形成独立音频时间定位；缺失、重叠或越界分段统一回退整段媒体定位。真实 Provider/WAV 浏览器验收仍待管理员配置受管服务后执行。
- 最新开发单元：`M5-08/M4-05` 已将首个媒体格式收敛为条件化 WAV 直传：仅存在策略相容的健康 ASR Provider 时才入队，Worker 先探测、再重核 Provider，最后生成可追溯转写节点；未配置真实 Provider 时仍明确拒绝，不制造失败任务。
- 最新验证单元：`M4-06` 已将本地视频容器的音轨和内嵌字幕流清单、文本型内嵌字幕 cue、受限 JPEG 关键帧及条件化第一音轨转写写入受控媒体节点；MP4 已可上传，关键帧可按时间回源，但不形成画面结论。
- 最新完成单元：`M5-00` 已建立无特权的内部 `AgentCoreAdapter` 与 20 条合成事件轨迹，作为 Pi 等候选的可替换基线。Pi 保持待供应链审查，OpenCode/Claude Code 仅作模式/公开规范参考。
- 最新开发单元：`M5-01/M5-02/M5-10/M5-11/M5-12` 已实现多轮会话、1–8 个知识范围 Binding、受管 `/knowledge/...` 虚拟路径、零 Embedding 检索、证据快照、SSE 运行阶段、可停止状态和完整工具轨迹；当前轮会带入有限的已完成历史以理解追问，但历史不构成证据或权限来源。范围可为整空间、已发布 Wiki 页面、已处理资料版本或已确认课程，检索在 EvidenceBundle 前过滤。真实 Provider 与完整已登录对话 E2E 仍在后续切片。
- 最新验证单元：`M5-12/M6-04` 已完成空间、Wiki 页面、ResourceVersion 和已确认 Course 的稳定 ID Binding；Course 在每轮只解析所选空间内的固定 CourseUnit 版本，不接受手工虚拟路径或动态课程集合。
- 最新验证单元：`M5-10` 已将每轮受管 Markdown-first 检索以 `knowledge.search` ToolCall 原子记录 Binding、计数、耗时和脱敏摘要；会话刷新后可显示工具状态，不保存问题或知识正文。
- 最新验证单元：`M5-10` 已接续 `knowledge.read`：只把同轮已过滤的 EvidenceBundle 片段提供给模型并记录读取计数，不读取整页 Wiki 或扩大页面/资料版本/Course 范围。
- 最新开发单元：`M5-06/M5-10` 会话页将 `wiki-query` 标记为对话内置、`wiki-lint` 标记为可安全执行 Worker 能力；其余安装的 Skill 显式显示为待接入执行器，并在控制面拒绝创建必然失败的 Run。
- 最新开发单元：`M5-06/M5-07` 已完成动态 TypeScript/Python CLI Skill 的准入、固定入口 registry、独立 UUID 目录及 fail-closed JSON I/O 第一层契约；原始 Blob、宿主路径、数据库凭据、网络和模型能力仍在首批动态执行中拒绝，子进程隔离与生成型学习 Skill 继续待接入。
- 最新开发单元：`M5-06/M5-07` 已接入 Linux Bubblewrap 的动态 CLI 运行器：强制新用户/挂载/PID/网络命名空间、受管只读输入/入口、仅 artifacts/tmp 可写、最小环境和超时终止；没有隔离运行时或在非 Linux 平台明确拒绝，绝不回退为宿主直接执行。内部 Worker 调用链已接通，管理端安装发现与 Linux 实际子进程验收仍后置。
- 最新验证单元：`M4-05/M4-06/M4-09` 媒体来源页已接通同一历史版本的字幕/转写节点和关键帧，点击均可时间跳转；它不增加关键帧 OCR 或画面理解，真实已登录媒体预览仍待补。
- 最新验证单元：`M5-11` 已将 `run.started`、`knowledge.search/read` 和 completed/failed/stopped 持久化为连续、脱敏的运行事件；会话所有者可通过 `GET /api/agent-runs/{runId}/events` 和 `Last-Event-ID` 跨进程重放，不会恢复模型调用或暴露问题、回答、证据/来源正文或路径。
- 执行边界：用户已批准按计划启动 M6-01/M6-03 的确定性选材与计划确认；正式产品启用仍需先通过 M3 门禁和相关 M4/M5 依赖。
- 主执行入口：[项目章程](spec/project-charter-v1.md) → [总体设计](design/system-architecture-v1.md) → [主实施计划](plan/master-implementation-plan-v1.md)。
